spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/postgres # 数据库地址
    username: java2  # 数据库用户名
    password: Java2  # 数据库密码
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    hibernate:
       ddl-auto: update
       #ddl-auto: create
    show-sql: true

server:
  port: 9090